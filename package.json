{
  "version": "1.0.0",
  "name": "parcel2-imagebug",
  "private": true,
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --no-source-maps",
    "debug": "parcel build src/index.html --dist-dir build/ --no-source-maps --no-minify --public-url ./build/ --no-cache"
  }
}